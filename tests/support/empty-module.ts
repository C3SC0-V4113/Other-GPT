// Stub for `server-only` / `client-only` marker packages in the test runner,
// which have no resolvable entry under Vite. They only enforce boundaries at
// build time and are inert at runtime.
export {};
